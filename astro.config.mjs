import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Since you mentioned using Vercel earlier, you can leave this config completely bare.
  // Vercel handles standard Astro static builds automatically!
  
  // Optional: If you ever decide to host this on GitHub Pages instead of Vercel, 
  // you would uncomment and update the lines below:
  // site: 'https://c4mdax.github.io',
  // base: '/21-years-fer-detalles',
});